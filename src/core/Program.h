#ifndef PROGRAM_H
#define PROGRAM_H

#include "system/window/Window.h"

/**
 * @brief Initializes and runs the entire application.
 *
 * This function is responsible for setting up the engine systems,
 * creating the window, initializing rendering resources, and running
 * the main loop of the program.
 */
void Init_Program();

#endif