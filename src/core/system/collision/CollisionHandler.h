#pragma once

#include "fractal_engine/graphics/Camera.h"
#include "../../objects/Cube.h"

using namespace fractal_engine::graphics;


void check_Collision(Camera &camera, Cube &cube){

    if (cameraPos.y == cube.getPosition().y)
    {
        
    }
    

}