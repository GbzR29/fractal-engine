#pragma once

// Include the actual Player implementation
// This file is in the source tree until we move it to a proper location
#include "../../src/core/system/player/Player.h"

namespace fractal_engine::scene {
    // Re-export Player class from the scene module
    // This temporarily imports Player which will be properly namespaced later
}
